server:
  port: ${PORT:9080}

spring:
  application:
    name: smart-bike
    version: 1
    description: Smart Bike Rental Backend (Docker)
    base-url: http://app:9080

  datasource:
    url: jdbc:postgresql://ep-summer-salad-aqlm9g8k-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require
    username: neondb_owner
    password: npg_X4iqTO5aQkFj
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: false
    database-platform: org.hibernate.dialect.PostgreSQLDialect

  mail:
    host: smtp.gmail.com
    port: 587
    username: biruksolomonmoges@gmail.com
    password: kbemdrrbdmkjxden
    from: ${SPRING_MAIL_FROM:noreply@smartbike.com}
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true

  security:
    user:
      name: admin
      password: admin123

admin:
  email: ${ADMIN_EMAIL:admin@smartbike.com}
  password: ${ADMIN_PASSWORD:Admin@123}
  name: ${ADMIN_NAME:System Administrator}
  phone: ${ADMIN_PHONE:+1234567890}

mqtt:
  broker: tcp://broker.emqx.io:1883
  client-id: smart-bike-spring-backend
  username: ""
  password: ""
  token: "1234"

jwt:
  secret: ${JWT_SECRET:mySecretKeyForSmartBikeRentalApplicationWithMinimum256BitsLength}
  expiration: ${JWT_EXPIRATION:86400000}

chapa:
  secret-key: ${CHAPA_SECRET_KEY:CHASECK_TEST-0Td4aInPwTC3s4rSLoM3xjcPQ1YV0hmX}
  webhook-secret: ${CHAPA_WEBHOOK_SECRET:changeme}
  enabled: ${CHAPA_ENABLED:true}

app:
  callback-url: ${APP_CALLBACK_URL:http://app:9080/api/payment/callback}

springdoc:
  api-docs:
    path: /v3/api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    try-it-out-enabled: true
    filter: true
  packages-to-scan: com.IoT.smart_bike_rental_backend.controller
  paths-to-match: /**
  show-actuator: false

management:
  endpoints:
    web:
      exposure:
        include: health,info
  endpoint:
    health:
      show-details: when-authorized

logging:
  level:
    root: INFO
    com.IoT.smart_bike_rental_backend: DEBUG
  file:
    name: /app/logs/application.log
